"use strict";

module.exports = class {
	constructor(maxCount){
		var unused = new Array(maxCount);
		for(var i=0; i<maxCount; ++i)
			unused[i] = i;
		this.unused = unused;
	}

	get(){
		return this.unused.pop();
	}

	put(uid){
		this.unused.push(uid);
	}
}